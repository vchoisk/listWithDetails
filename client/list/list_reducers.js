
const listReducer = (state = [], action) => {
  switch (action) {
    // case 'FILTER':
    //   return state;
    default:
      return state;
  }
}

const listReducers = {
  listReducer
}

export default listReducers;